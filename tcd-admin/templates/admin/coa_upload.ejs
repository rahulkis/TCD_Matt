<section class="content">
    <!-- Small boxes (Stat box) -->
    <div class="row">
        <div class="col-xs-12">
            <%- include ('./partials/admin_messages') %>
            <div class="box">
                <div class="box-header with-border">
                    <h3 class="box-title">COA Information</h3>
                    <div class="pull-right">
                        <a href="/admin/coa" class="btn btn-warning btn-xs" style="color:#fff"><i class="fa fa-arrow-left" aria-hidden="true"></i></a>
                    </div>
                </div>
                <!-- /.box-header -->
                <div class="box-body">
                    <form class="form-material" method="post" enctype="multipart/form-data" id="coaUploadForm" action="/admin/parse-coa">
                        <div class="row">
                            <div class="col-md-6">
                                <div class="form-group">
                                    <label>Upload COA Image/PDF<span class="required">*</span></label>
                                    <input type="file" name="coa" class="file" >
                                </div>
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-md-3">
                                <div class="form-group">
                                    <label style="width:100%">&nbsp;</label>
                                    <button type="submit" id="coaUpload" class="btn btn-primary" id="coaUploadBtn">Upload</button>
                                    <!-- <input type="submit" name="coaUpload" value="Submit"/> -->
                                </div>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</section>
<%- contentFor('page_js') %>
<script>
    $(document).ready(function () {
        $("#coaUploadForm").validate({
            rules:{
                coa:{
                    required:true,
					extension: "jpg,jpeg,png,pdf"
                }
            },
			messages:{
				coa: {
                    required:"Please add an COA image/pdf",
					extension: "Only image type extensions (jpg,jpeg,png) are allowed"
				}
			},
            errorElement:'div'
        })
    
        /*$('#coaUploadForm').submit(function() {
            //$("#status").empty().text("File is uploading...");
            $(this).ajaxSubmit({
                error: function(xhr) {
                    status('Error: ' + xhr.status);
                },
                success: function(response) {
                    //$("#status").empty().text(response);
                    console.log(response);
                }
            });
            //Code to disable the page refresh.
            return false;
        }); */
    });
</script>